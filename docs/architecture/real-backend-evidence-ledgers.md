# Real-backend evidence ledgers

Phase 2H ledgers bind browser actions, allowlisted requests, exact operation IDs, backend state sequences, semantic language results, canonical lesson identities, artefact revisions, persistence readback, concurrency context, and budget consumption.

Backend, provider, database, normal-profile, and promotion verdicts remain separate.
