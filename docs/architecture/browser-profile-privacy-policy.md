# Browser-profile privacy policy

Guard discovers only bounded Chrome product roots. It snapshots Local State,
Preferences, Secure Preferences, and identified extension static metadata into
Guard-owned `.work`. Sensitive databases, application storage, page content,
and unrelated extension details are prohibited. Every copied file is hashed
before and after copying; a changing source rejects the snapshot.
