# Transactional Chrome extension promotion

The design orders baseline, rollback, successor, ID, approval, Chrome-closed,
same-volume staging, on-disk verification, startup, runtime attestation, golden
journeys, commit, and observation. Failure restores and re-verifies the exact baseline.
