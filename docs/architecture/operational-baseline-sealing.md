# Operational baseline sealing

Guard copies the exact configured extension into an ignored baseline workspace,
proves payload equality, signs a detached attestation, and adds runtime readback
only to the delivery layer. Behaviour, normal-profile runtime, and source
reproducibility remain separate UNPROVEN claims.
