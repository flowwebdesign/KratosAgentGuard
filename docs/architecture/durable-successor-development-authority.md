# Durable successor-development authority

The exact path `C:\Users\floww\Documents\Itzako Extension Successor` is an independent repository imported from a signed bundle. It has a separate object database, no alternates, no linked worktree, and no remote. An authority descriptor binds its base, branch, sole fix, and zero promotion authority.
