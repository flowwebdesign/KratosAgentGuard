# Itzako Stale Extension Candidate

The target `Study_master/extension` directory contains `buildInfo.js` naming
commit `950e204`, while current observed source is `b7529f00…`. That historical
metadata contradicts current source-to-build identity and remains
`SOURCE_TO_BUILD_LINK_CONTRADICTED`.

Phase 2B does not claim whether those bytes are live-loaded. The sealed candidate
copies observed source bytes into a separate Guard-owned workspace and adds a
detached attestation. Historical metadata remains an input limitation, not a
trusted provenance claim.
