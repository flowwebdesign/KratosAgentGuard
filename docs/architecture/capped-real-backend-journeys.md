# Capped real-backend journeys

`RealBackendBudget` records immutable hard caps for sessions, lessons, operations, repairs, regeneration, duplicate attempts, duration, unexpected row deltas, provider calls, and provider cost.

The plan checks all identity gates before dispatch. A blocked plan creates evidence with zero operations and never launches a browser profile.
