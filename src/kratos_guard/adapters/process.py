"""Passive TCP-listener observations."""

import socket


def port_accepts_connection(host: str, port: int, timeout: float = 0.2) -> bool:
    try:
        with socket.create_connection((host, port), timeout=timeout):
            return True
    except OSError:
        return False
