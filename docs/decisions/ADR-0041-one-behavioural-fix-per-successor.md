# ADR-0041: One behavioural fix per successor

## Decision

Each successor lane implements one named behavioural contract.
## Consequence

Every runtime delta is attributable and independently reversible.
