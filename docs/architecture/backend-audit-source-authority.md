# Backend audit source authority

Phase 2I uses `C:\Users\floww\Documents\Itzako Backend Audit Successor` as an independent, remote-free Git repository based on canonical commit `de4f09113388aac14b7b0f062878c8fb62c2e67f`.

Authority requires its own Git directory and object database, no alternates, no linked worktree relationship, no hardlinked objects, a clean audit branch, and no push URL. The canonical Study Pilot repository remains a read-only lineage source.

The audit delta is limited to authentication, deterministic provider scenarios, writer-lease validation, database provisioning, and a non-secret identity endpoint. Ordinary non-audit behaviour remains unchanged. A signed reproducible build attestation and Git bundle link the baseline, audit commit, packaged bytes, and Guard trust root without granting promotion authority.
