# Behavioural-successor attestations

The signed attestation binds the clean development commit, component input, payload, builder, signing key, and reproducibility. Runtime readback independently checks the packaged attestation from the loaded extension origin.
