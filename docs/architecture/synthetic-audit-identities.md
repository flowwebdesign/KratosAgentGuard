# Synthetic audit identities

A synthetic identity must pre-exist, expose a scoped authentication method, and be independently marked as non-learner-owned. Guard never creates a normal account, copies cookies, or treats a convenient local session as synthetic.

Every permitted record is additionally bound to one `KAG-2H-*` run namespace.
