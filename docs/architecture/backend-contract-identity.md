# Backend contract identity

Backend identity combines listener PID, executable, command line, working directory, runtime manifest, protected digest, migration head, OpenAPI hash, relevant route hashes, provider mode, and pre/post contract stability.

HTTP health alone is supporting evidence. Auto-reload, route loss, PID mismatch, or contract hash drift prevents a proven identity.
