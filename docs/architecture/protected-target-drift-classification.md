# Protected-target drift classification

Phase 2H compares an exact prior witness with a fresh bounded witness, classifies only the changed paths, and never infers attribution. Resolved paths are checked against active process working directories, mounted sources, and the configured extension root.

Runtime overlap blocks dispatch. Unrelated or evidence-only drift remains visible but does not invalidate independent Guard work. Execution-critical unknown paths also block.
