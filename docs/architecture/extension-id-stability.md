# Extension-ID stability

Static manifest `key` evidence is public identity material. Guard never searches
for private keys. Two exact copies are loaded from different Guard-owned paths;
their isolated IDs are compared with each other and the normal-profile ID.
