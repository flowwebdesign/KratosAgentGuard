# Current-runtime evidence

The evidence ladder is configured extension, persisted registration, observed
active worker, and runtime-returned attestation. No lower rung satisfies a higher
gate. Phase 2D does not enable debugging; without a pre-existing independently
authorised endpoint, worker and runtime-attestation gates remain UNPROVEN.
