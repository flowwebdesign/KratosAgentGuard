# Configured-extension identity

Profile selection precedes extension selection. Configuration evidence yields an
extension ID, install type, canonical path, manifest facts, and deterministic
static payload. Candidate equivalence requires payload or signed-attestation
identity; name and version are supporting facts only.
